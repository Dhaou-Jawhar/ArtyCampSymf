{% extends 'baseFront.html.twig' %}

{% block body %}
<article>
    <h1>{{ article.nomA }}</h1>
    <p>{{ article.descriptionA }}</p>
</article>
    <a href=" {{ path('app_articles_modif',{idArticle: article.idArticle}) }} ">Modifier</a>
    <a href="{{ path('app_home') }}">Retour</a>
{% endblock %}